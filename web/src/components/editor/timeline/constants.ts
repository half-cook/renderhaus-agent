// Ported from spikes/timeline-render (§10 in that spike's README) — sized
// down for our slimmer track rows rather than the spike's test-harness ones.
export const TRACK_HEIGHT = 40;
export const TRACK_GAP = 4;
export const RULER_HEIGHT = 20;
export const INITIAL_PX_PER_SEC = 60;
export const MIN_PX_PER_SEC = 5;
export const MAX_PX_PER_SEC = 400;
