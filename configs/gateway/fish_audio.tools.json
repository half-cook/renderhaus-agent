[
  {
    "name": "list_voices",
    "description": "List starter Fish Audio voices, plus popular public models when live.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "generate_speech",
    "description": "Generate Fish Audio speech and save a local audio file when live.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        },
        "voice": {
          "type": "string"
        },
        "output_format": {
          "enum": [
            "wav",
            "mp3"
          ],
          "type": "string"
        },
        "model": {
          "enum": [
            "s1",
            "s2-pro",
            "s2.1-pro",
            "s2.1-pro-free"
          ],
          "type": "string"
        }
      },
      "required": [
        "text"
      ]
    }
  },
  {
    "name": "estimate_tts_cost",
    "description": "Return simple character-count data for Fish Audio TTS cost planning.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ]
    }
  }
]
